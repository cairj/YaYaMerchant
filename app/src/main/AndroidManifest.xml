<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yaya.merchant">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name=".application.MerchantApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme" >

        <activity android:name=".activity.login.LoginActivity"
            android:theme="@style/GrayActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.login.InputUserNameActivity"
            android:theme="@style/GrayActionBar"/>
        <activity android:name=".activity.login.ChangePasswordActivity"
            android:theme="@style/GrayActionBar"/>
        <activity android:name=".activity.login.ProvingActivity"
            android:theme="@style/GrayActionBar"/>
        <activity android:name=".activity.MainActivity"
            android:theme="@style/ImageTranslucentTheme"/>

    </application>
</manifest>
